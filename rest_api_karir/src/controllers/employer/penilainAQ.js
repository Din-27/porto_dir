const programNilai = (quiter, camper, climber) => {
    let score
    if (quiter.length === 7 && camper.length === 1 && climber.length === 7) {
        score = 'CAMPER'
    }
    if (quiter.length === 6 && camper.length === 3 && climber.length === 6) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 5 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 6 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 6 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 6 && camper.length === 6 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 7 && climber.length === 6) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 7 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 7 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 7 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 6 && camper.length === 7 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 7 && camper.length === 7 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 8 && climber.length === 7) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 8 && climber.length === 6) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 8 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 8 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 8 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 8 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 6 && camper.length === 8 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 7 && camper.length === 8 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 9 && climber.length === 6) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 9 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 9 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 9 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 9 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 9 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 6 && camper.length === 9 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 10 && climber.length === 5) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 10 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 10 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 10 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 10 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 5 && camper.length === 10 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 11 && climber.length === 4) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 11 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 11 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 11 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 11 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 12 && climber.length === 3) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 12 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 12 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 12 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 13 && climber.length === 2) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 13 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 2 && camper.length === 13 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 14 && climber.length === 1) {
        score = 'CAMPER'
    }
    if (quiter.length === 1 && camper.length === 14 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 0 && camper.length === 15 && climber.length === 0) {
        score = 'CAMPER'
    }
    if (quiter.length === 3 && camper.length === 6 && climber.length === 6) {
        score = 'CAMPER'
    }
    if (quiter.length === 4 && camper.length === 5 && climber.length === 6) {
        score = 'CLIMBER'
    }
    if (quiter.length === 5 && camper.length === 4 && climber.length === 6) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 7 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 6 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 5 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 4 && camper.length === 4 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 5 && camper.length === 3 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 6 && camper.length === 2 && climber.length === 7) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 7 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 6 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 5 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 4 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 4 && camper.length === 3 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 5 && camper.length === 2 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 6 && camper.length === 1 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 7 && camper.length === 0 && climber.length === 8) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 6 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 5 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 4 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 3 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 4 && camper.length === 2 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 5 && camper.length === 1 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 6 && camper.length === 0 && climber.length === 9) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 5 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 4 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 3 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 2 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 4 && camper.length === 1 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 5 && camper.length === 0 && climber.length === 10) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 4 && climber.length === 11) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 3 && climber.length === 11) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 2 && climber.length === 11) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 1 && climber.length === 11) {
        score = 'CLIMBER'
    }
    if (quiter.length === 4 && camper.length === 0 && climber.length === 11) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 3 && climber.length === 12) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 2 && climber.length === 12) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 1 && climber.length === 12) {
        score = 'CLIMBER'
    }
    if (quiter.length === 3 && camper.length === 0 && climber.length === 12) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 2 && climber.length === 13) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 1 && climber.length === 13) {
        score = 'CLIMBER'
    }
    if (quiter.length === 2 && camper.length === 0 && climber.length === 13) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 1 && climber.length === 14) {
        score = 'CLIMBER'
    }
    if (quiter.length === 1 && camper.length === 0 && climber.length === 13) {
        score = 'CLIMBER'
    }
    if (quiter.length === 0 && camper.length === 0 && climber.length === 15) {
        score = 'CLIMBER'
    }
    if (quiter.length === 6 && camper.length === 4 && climber.length === 5) {
        score = 'QUITER'
    }
    if (quiter.length === 6 && camper.length === 5 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 7 && camper.length === 2 && climber.length === 6) {
        score = 'QUITER'
    }
    if (quiter.length === 7 && camper.length === 3 && climber.length === 5) {
        score = 'QUITER'
    }
    if (quiter.length === 7 && camper.length === 4 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 7 && camper.length === 5 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 7 && camper.length === 6 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 0 && climber.length === 7) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 1 && climber.length === 6) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 2 && climber.length === 5) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 3 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 4 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 5 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 6 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 8 && camper.length === 7 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 0 && climber.length === 6) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 1 && climber.length === 5) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 2 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 3 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 4 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 5 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 9 && camper.length === 6 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 0 && climber.length === 5) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 1 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 2 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 3 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 4 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 10 && camper.length === 5 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 11 && camper.length === 0 && climber.length === 4) {
        score = 'QUITER'
    }
    if (quiter.length === 11 && camper.length === 1 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 11 && camper.length === 2 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 11 && camper.length === 3 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 11 && camper.length === 4 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 12 && camper.length === 0 && climber.length === 3) {
        score = 'QUITER'
    }
    if (quiter.length === 12 && camper.length === 1 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 12 && camper.length === 2 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 12 && camper.length === 3 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 13 && camper.length === 0 && climber.length === 2) {
        score = 'QUITER'
    }
    if (quiter.length === 13 && camper.length === 1 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 13 && camper.length === 2 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 14 && camper.length === 0 && climber.length === 1) {
        score = 'QUITER'
    }
    if (quiter.length === 14 && camper.length === 1 && climber.length === 0) {
        score = 'QUITER'
    }
    if (quiter.length === 15 && camper.length === 0 && climber.length === 0) {
        score = 'QUITER'
    }
    return score
}

module.exports = { programNilai }